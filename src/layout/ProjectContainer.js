import './ProjectContainer.css'

const ProjectContainer = (props) => {
    return (
      <div className='project-container'>{props.children}
              
              
    
          
      </div>
    );
  }
  
  export default ProjectContainer;
  